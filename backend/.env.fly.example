# ============================================
# Fly.io Environment Configuration Example
# Copy to .env on Fly.io after deployment
# ============================================

# Node Environment
NODE_ENV=production

# Database (via Fly Postgres)
DATABASE_URL=postgresql://YOUR_DB_USER:YOUR_DB_PASSWORD@YOUR_HOST:5432/YOUR_DB

# Redis (via Fly Redis or Upstash)
REDIS_URL=redis://redis-name.internal:6379

# API Keys (set via: fly secrets set KEY=value)
OPENAI_API_KEY=sk-...
ANTHROPIC_API_KEY=sk-ant-...

# México-specific Configuration
DEFAULT_LANGUAGE=es-MX
DEFAULT_TIMEZONE=America/Mexico_City

# Application Settings
PORT=8080
CORS_ORIGIN=https://ojea-mexico.netlify.app

# Fly.io Platform
FLY_APP_NAME=ojea-api
FLY_REGION=qro

# Note: Actual secrets should be set via `fly secrets set`
# Never commit real credentials to version control